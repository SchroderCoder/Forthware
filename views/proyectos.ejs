<%- include('includes/head.ejs') %>
<h1>Entraste a proyectos</h1>

<h3>Proyectos</h3>
    <% if (proyectos.length > 0) { %>
        <div>
            <% for (let  p of proyectos) { %>
                <a href="/proyectos/editarProyecto/<%= p.id_proyecto %>" ><%= p.nombre %></a>
                <br>
            <% } %>
            </div>
    <% } else { %>
        <h2>No hay proyectos registrados</h2>
    <% } %>  
<br>
<h3>Etiquetas</h3>
    <% if (etiquetas.length > 0) { %>
        <div>
            <% for (let  e of etiquetas) { %>
                <a href="/proyectos/editarEtiqueta/<%= e.id_proyecto %>" ><%= e.nombre %></a>
                <br>
            <% } %>
            </div>
    <% } else { %>
        <h2>No hay proyectos registrados</h2>
    <% } %>  
<br>
<div>   
        <% if (privilegios.indexOf('Crear proyecto') > -1) { %>
                <a href="/proyectos/crearProyecto" button type="button">Crear proyecto</a>
                <!-- <a href="/proyectos/crearEtiqueta" button type="button">Crear etiqueta</button> -->
        <% } %>  
</div>

<%- include('includes/foot.ejs') %>